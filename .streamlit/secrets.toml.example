# Rename this file to secrets.toml and fill in your values.
# secrets.toml is gitignored and never committed to source control.
#
# Streamlit reads this file automatically at runtime.
# On a hosted platform (e.g. Streamlit Community Cloud) use the platform's
# Secrets UI instead of this file.

# OpenAI API key — enables AI-generated narratives in the AI Career Sim tab.
# Leave blank (or omit the key) to use the built-in story templates.
OPENAI_API_KEY = ""
